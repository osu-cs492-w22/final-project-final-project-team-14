<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory android:title="@string/pref_order_by_anime">
        <ListPreference
            android:title="@string/pref_order_by_name"
            android:key="@string/pref_order_by_key"
            android:entries="@array/pref_sort_entries"
            android:entryValues="@array/pref_sort_val"
            android:defaultValue="@string/pref_order_by_default"
            android:dialogTitle="@string/pref_order_by_title"
            app:useSimpleSummaryProvider="true" />
    </PreferenceCategory>

</PreferenceScreen>